language: java
sudo: false

jdk:
  - oraclejdk8
cache:
  directories:
    - $HOME/.m2/repository
install:
  - mvn clean install

script:
  - mvn clean verify
after_success:
  - mvn clean test jacoco:report coveralls:report

notifications:
  email:
    on_success: never
    on_failure: never
